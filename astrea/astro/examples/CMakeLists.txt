set(CMAKE_INSTALL_MESSAGE LAZY)

# Get sources
set(BASE ${CMAKE_CURRENT_SOURCE_DIR})
file(GLOB_RECURSE EXAMPLE_SOURCES CONFIGURE_DEPENDS ${BASE}/**/main.cpp)
build_examples(${PROJECT_NAME} "${EXAMPLE_SOURCES}")